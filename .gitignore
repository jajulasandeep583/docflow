# Python
__pycache__/
*.pyc
*.egg-info/
.eggs/
dist/
build/

# Node / frontend build
node_modules/
.frontend-build/

# Node build cache only — built output IS committed so `bench get-app` works
# without needing Node on the target server.
frontend/node_modules/

# Editor / OS
.DS_Store
.vscode/
.idea/
*.log
